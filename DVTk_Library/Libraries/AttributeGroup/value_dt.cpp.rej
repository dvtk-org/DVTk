diff a/DVTk_Library/Libraries/AttributeGroup/value_dt.cpp b/DVTk_Library/Libraries/AttributeGroup/value_dt.cpp	(rejected hunks)
@@ -192,12 +192,21 @@
         }
 
         // Deal with offset first. Note that an offset can not be at the
         // beginning of a string.
         if (offset_index != 0)
         {
+            // Check if we have exactly 4 digits for timezone offset (�HHMM)
+            int remaining_length = syntax.length() - offset_index;
+            if (remaining_length != 4)
+            {
+                sprintf (message, "invalid optional offset suffix - expected 4 digits (�HHMM), found %d digits", remaining_length);
+                messages->AddMessage (VAL_RULE_D_DT_2, message);
+                return MSG_ERROR;
+            }
+            
             if (syntax.find("nnnn", offset_index) != offset_index)
             {
                 sprintf (message, "invalid optional offset suffix");
                 messages->AddMessage (VAL_RULE_D_DT_2, message);
 				return MSG_ERROR;
             }
